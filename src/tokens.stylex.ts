import { defineVars } from '@stylexjs/stylex';

export const spacing = defineVars({
  xsmall: '4px',
  small: '8px',
});

export const colors = defineVars({
  primary: 'brown',
  secondary: 'gray',
});
